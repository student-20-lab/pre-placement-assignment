#include <stdio.h>

int main() {
    int r, c, mat[100][100], count = 0;
    scanf("%d %d", &r, &c);
    for (int i = 0; i < r; i++)
        for (int j = 0; j < c; j++) {
            scanf("%d", &mat[i][j]);
            if (mat[i][j] == 0) count++;
        }

    if (count > (r * c) / 2)
        printf("Sparse matrix");
    else
        printf("Not a sparse matrix");

    return 0;
}

